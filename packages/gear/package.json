{
  "name": "gear",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "index.js",
  "typings": "index.d.ts",
  "scripts": {
    "test:type": "tsc -p tsconfig.lib.json --noEmit && tsc -p tsconfig.test.json --noEmit"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
